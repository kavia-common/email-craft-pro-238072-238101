[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
asyncio_mode = auto

# Ensure `import src.*` works under plain `pytest` by adding the backend root
# (the folder that contains `src/`) to sys.path.
pythonpath = .
